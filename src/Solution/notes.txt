--- ON HOLD ---
    spotify
        https://eltechs.com/run-spotify-on-raspberry-pi/
        
--- TODO ---
    IPTV

    Tv control goed toepassen
        pi_is_active bijwerken door interpreten cec messages
        Also adjust NOOBS /boot/config.txt with hdmi_ignore_cec_init=1

    Check security for slave devices

    utp

    Fix back going a page back instead of to base menu

    Refactor gas/power usage graphs
        Buffer data so we only have to request once ( maybe remove if older than x? )
        Create distinction between flow and graph data.
            Flow = consumption per 5 minutes -> line graph
            Graph = consumption per interval -> historical data -> bar graph
        Add selection tools
            Quick selections: Today, This month, This year
            Add start/end time selectors

    Als device group delete dan ook rule voor die deleten?

    mobile-lock screen voor play/pause again?

    States maken?
        Film -> gedimde verlichting
        Normaal
        Sleep
        etc